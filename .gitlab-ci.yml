image: rocker/r-ver:4.0.0

pages:
  script:
    - Rscript -e "install.packages('pkgdown')"
    - Rscript -e "pkgdown::build_site()"
    - mv docs public
  artifacts:
    paths:
      - public
  only:
    - master